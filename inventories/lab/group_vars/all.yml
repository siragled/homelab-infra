---
# Global configuration for homelab environment
homelab_domain: "homelab.local"
timezone: "UTC"

# SSH configuration
ansible_ssh_common_args: '-o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null'

# Common packages
common_packages:
  - htop
  - vim
  - curl
  - wget
  - git
